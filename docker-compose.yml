services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: crm
      POSTGRES_PASSWORD: crm
      POSTGRES_DB: crm_viet
    ports:
      - "5432:5432"
    volumes:
      - crm_pg_data:/var/lib/postgresql/data

volumes:
  crm_pg_data:
